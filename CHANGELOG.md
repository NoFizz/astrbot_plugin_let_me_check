# Changelog

## [2.2.1] - 2026-08-11

### Fixed
- 修复转发"会话记忆"在 AstrBot 保存本轮历史后被整体覆盖而丢失的问题：转发上下文现随本轮用户消息由 AstrBot 一并持久化（图片直通内容仍为临时，不写入历史）。
- 修复同一批次多条暂存转发并行解析时协议拉取并发上限被放大（每条各自 `Semaphore(8)`，10 条可放大至约 80 并发）的问题：同批次现共享同一并发限制。
- 修复远程顶层转发内容再次引用自身时被重复拉取的问题：顶层转发 ID 现计入共享去重集合与总拉取闸门。
- 修复 `max_messages`/`max_nested_depth`/`parse_nested_forward`/`image_caption_provider_id`/`image_caption_prompt` 等配置在手工编辑或迁移污染（字符串、负数、非字符串）时可能触发类型异常的问题：非法值现安全回退默认并记录 warning。

### Changed
- 移除从未生效的 `model_config.provider_id` 配置项（AstrBot 当前钩子机制下无法在插件内切换主对话模型）。
- 单次触发注入新增总内容预算（约 2 万字符 / 20 张图片），超出部分按入队顺序截断并在注入内容末尾明确标注，日志记录保留/总条数。
- 日志统一恢复 `[让我康康]` 插件名前缀，便于混合日志定位。
- 白名单会话不匹配时记录 debug 日志（此前静默跳过，难以排查）；WebUI 提示与 README 明确要求白名单填写完整 UMO（仅填群号/QQ 号不会生效）。
- README 移除独立"环境要求"章节，功能特性、配置表、数据存储与隐私等文案与当前实际行为对齐。

### Removed
- 移除 `history.py` 模块（会话历史持久化改由 AstrBot Core 在保存本轮对话时一并完成，插件侧写库会被 Core 整体覆写）。
